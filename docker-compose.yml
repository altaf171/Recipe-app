services:
  app:
    container_name: recipe-app_api
    image: recipe-app_api_img
    build:
      context: .
    ports:
      - 8000:8000
    volumes:
      - ./app:/app
    command: >
      sh -c "python manage.py runserver 0.0.0.0:8000"

    